# Runtime pipeline output (per CLAUDE.md: jobs/ is gitignored)
/jobs/

# Model weights / downloaded checkpoints
/models/
*.onnx
*.pt
*.pth

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Test-set derived artifacts (source images are tracked; debug overlays are regenerable)
/testset/debug/*.png

# Raw capture drop folder (originals copied into testset/ with canonical ids)
/1/

# Stray browser/drive downloads (never commit into the anchor repo)
drive-download-*.zip
