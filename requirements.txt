# bookscan Python dependencies (Python 3.11+).
# Pin loosely for now; tighten once Gate 1 is proven.

# --- Shared schema ---
pydantic>=2.6

# --- Config / IO ---
pyyaml>=6.0
Pillow>=10.0          # image IO / normalization (Stage 00)

# --- OCR + Gate 1 harness ---
pytesseract>=0.3.10   # optional convenience; harness may call tesseract via subprocess
opencv-python>=4.9    # preprocessing, split, fuse, dewarp helpers
scipy>=1.11           # Stage 03 dewarp: band-projection peak-linking baselines
rapidfuzz>=3.6        # word/char alignment + edit distance for accuracy metrics
jiwer>=3.0            # WER alignment ops (alternative to rapidfuzz editops)
matplotlib>=3.8       # confidence histograms / debug plots
numpy>=1.26

# --- Stage 03 dewarp: UVDoc neural unwarper (GPU) ---
# On Blackwell (RTX 50xx) install the CUDA build explicitly, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.6             # weights_only=True default is relied on for the checkpoint
torchvision>=0.21

# --- Stage 04 layout: DocLayout-YOLO detector (GPU) ---
# Checkpoint (~40 MB) is gitignored; fetch into models/doclayout_yolo/ from HF
# 'juliozhao/DocLayout-YOLO-DocStructBench'. NB this pulls opencv-python-headless
# alongside opencv-python — harmless here (pipeline only writes images, no GUI).
doclayout-yolo>=0.0.4
huggingface_hub>=0.20   # checkpoint download

# --- Second-opinion OCR (Bulgarian) ---
easyocr>=1.7

# --- Later gates (kept here for a single install surface; unused until then) ---
# rawpy>=0.19         # Stage 00 RAW decode
# weasyprint>=61      # Stage 07 HTML -> PDF
