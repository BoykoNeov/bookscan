"""bookscan harness / debug tooling.

Runnable via ``python -m tools.<name>``. Tools stay INDEPENDENT of
``pipeline/`` so they remain usable as regression checks whenever OCR
settings or models change (e.g. ``tools.gate1_harness``).
"""
